# Introduction_to_State_Machine
入门状态机
任务：
| 你的操作 | 板子响应 |
|---------|---------|
| **短按一下**（按下松开 < 500ms） | LED 亮/灭 切换 |
| **长按**（按住 ≥ 500ms） | 通过串口打印出 help 命令列表 |
要求：
	1.用 enum 定义三个状态
	2.用 switch-case 或查表法实现状态转移
	3.用 HAL_GetTick() 做非阻塞计时（你已经会了）
